#include<iostream>
#include<vector>
#include<string> 
#include<math.h>

using namespace std;

class Solution {
public:
    string customSortString(string order, string str) {
        return "";
    }
};

int main () {
    // Solution sol;
    // string x, y;
// 
    // x = "cba";
    // y = "abcd";
    // cout << sol.customSortString(x, y) << endl;
    cout << log(1.0) / log(2.0) << endl;
    cout << log(4.0) / log(2.0) << endl;

    return 0;
}